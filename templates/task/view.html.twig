{% extends 'base.html.twig' %}

{% block body %}
<div class="container mx-auto p-8">
    <h1 class="text-2xl font-bold mb-6">Détails de la tâche</h1>
    <div class="bg-white shadow-md rounded p-6">
        <h2 class="text-lg font-semibold">Titre : {{ task.title }}</h2>
        <p class="mt-4">Description :</p>
        <p class="text-gray-700">{{ task.description }}</p>
        <div class="mt-6">
            <a href="{{ path('task_index') }}" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
